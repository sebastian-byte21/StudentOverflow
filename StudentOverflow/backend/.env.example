# Copy to .env and adjust values
ENV=development
# Example for PostgreSQL (recommended):
# DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@HOST:PORT/DB_NAME
# Example for MySQL:
# DATABASE_URL=mysql+pymysql://USER:PASSWORD@HOST:PORT/DB_NAME
# Example for SQLite (default if unset):
# DATABASE_URL=sqlite:///./studentoverflow.db